"""Example feature module to illustrate per-feature layering (domain/application/infrastructure).

Replace `example_feature` with your bounded context or feature name and extend the
contents under `domain`, `application`, and `infrastructure` accordingly.
"""
